{
  "buildCommand": "npm --prefix web-dashboard run build",
  "outputDirectory": "web-dashboard/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
